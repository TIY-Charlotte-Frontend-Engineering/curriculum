let _ = require('lodash');
window.jQuery = require('jquery');
let $ = window.jQuery;

let jq = require('jquery-ui');

console.log(Object.keys($));

let moderator_name = 'Shelby';

window.addEventListener('load', function () {
    // $('li').draggable();
    // $('body').append('<h2>Comment by ' + moderator_name + '</h2>');
    // $('li').on('click', function () {
    //     console.log('you clicked er');
    // })






    // console.log('oh boy');

/*
    // Range gives us numbers from (lower to upper)
    let nums = _.range(1, 101);

    let folks = ['Gladys', 'Jeb', 'Franklin', 'Cletus'];
    // Does EVERY element of folks return true?
    let allArePeople = _.every(folks, isHuman);
    // Do SOME of these elements return true?
    let anyArePeople = _.some(folks, isHuman);

    // sample

    // shuffle
    // partition
    let doods = [1, 5, 6, 7, 8, 9];
    let splitz = _.partition(doods, function (num) {
        if (num > 5) {
            return true;
        } else {
            return false;
        }
    });
    console.log(splitz);
    */
});
